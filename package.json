{
  "name": "emmanuel-tech-simplified-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/audit-links.mjs",
    "audit:responsive": "node tools/responsive-audit.mjs",
    "lint": "node --check assets/payment.js && node --check api/_lib/paystack.js && node --check api/paystack/initialize.js && node --check api/paystack/verify.js && node --check api/paystack/webhook.js",
    "test": "node --test tests/*.test.js"
  }
}
